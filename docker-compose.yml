version: "3"

services: 
    git-deployer-app:
        # use Dockerfile in this context repo
        build: .
        environment: 
            FLASK_APP: "app.py"
            FLASK_ENV: "development"
        ports:
            - "5000:5000"